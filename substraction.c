#include <stdio.h>
int main() {
    int num1,num2,sub;
    printf("enter any two numbers:");
    scanf("%d %d", &num1, &num2);
    sub = num1-num2;
    printf("substraction=%d",sub);
    return 0;
}